{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "publish": "lerna publish",
    "dev": "cd packages/stencil-library && npm run start"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^20.10.4",
    "lerna": "^8.0.0",
    "typescript": "^5.3.3"
  }
}
